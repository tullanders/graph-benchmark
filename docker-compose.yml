services:
  neo4j:
    image: neo4j:5.26.29-community
    environment:
      NEO4J_AUTH: neo4j/benchmark
      NEO4J_PLUGINS: '["graph-data-science"]'
      NEO4J_server_memory_pagecache_size: 1G
      NEO4J_server_memory_heap_max__size: 1500m
      # GDS procedures must be explicitly allowed in community
      NEO4J_dbms_security_procedures_unrestricted: "gds.*"
    volumes:
      - ./data:/var/lib/neo4j/import
    ports:
      - "7474:7474"
      - "7687:7687"
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:7474 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  memgraph:
    image: memgraph/memgraph-mage:3.12.0
    command:
      - "--memory-limit=4096"
      - "--log-level=WARNING"
      - "--query-execution-timeout-sec=300"
    volumes:
      - ./data:/usr/lib/memgraph/import-data:ro
    ports:
      - "7688:7687"
    healthcheck:
      # bash TCP check — nc is not guaranteed in this image
      test: ["CMD-SHELL", "timeout 1 bash -c 'cat < /dev/null > /dev/tcp/localhost/7687' || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s

  memgraph-lab:
    image: memgraph/lab:latest
    environment:
      QUICK_CONNECT_MG_HOST: memgraph
      QUICK_CONNECT_MG_PORT: 7687
    ports:
      - "3000:3000"
    depends_on:
      memgraph:
        condition: service_healthy

  arcadedb:
    image: arcadedata/arcadedb:latest
    environment:
      JAVA_OPTS: "-Xms512m -Xmx2g -Darcadedb.server.rootPassword=benchmark -Darcadedb.opencypher.loadCsv.allowFileUrls=true"      
    volumes:
      - ./data:/home/arcadedb/import:ro
    ports:
      - "2480:2480"
      - "2424:2424"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:2480/api/v1/ready || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 15s
